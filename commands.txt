# Deploy

git push live gh-pages:master

# Watch
bundle exec jekyll serve --watch

# Watch (drafts included)

bundle exec jekyll serve --watch --drafts

# Publish

1. Rename and move to _posts
2. Remove 'draft: true' from frontmatter
3. Deploy
4. Tweet
5. Add twitter_url to frontmatter
6. Redeploy
